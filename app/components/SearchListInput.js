import React, {Component} from 'react';
import TextInput from 'react-native';

class SearchListInput extends Component {
	constructor(props){
		super(props)
		this.state = {
			searchText = 
		}
	}
	render() {
		<TextInput style={styles.textInput} />
	}
}

const styles = StyleSheet.create({
 textInput:{
    height: 30,
    borderColor: 'blue',
    borderWidth: 1
  }
});